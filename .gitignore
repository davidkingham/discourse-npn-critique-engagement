node_modules/
.DS_Store
*.log
.bundle/
vendor/bundle/
tmp/
coverage/
.rspec_status

# Redis writes its snapshot into whatever directory it was started from, so
# a dev session run from here leaves one behind. Core ignores the same file
# at its own root.
/dump.rdb
